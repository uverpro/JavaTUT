// Write a function goodbye(name)
// that takes in a string name and returns a string saying bye to that name.

function goodbye (name) {
    return "Bye " + name
};

console.log(goodbye("Felicia"));
