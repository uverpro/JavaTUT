// 1.
let adder = (num1, num2) => {
  let add = num1 + num2;
  return add;
};

// 2.
let addFive = num => {
  let add = num + 5;
  return add;
};

// 3.
let addFive2 = num => num + 5;

// 4.
const addFive3 = num => {
  const add = num + 5;
  add;
};
